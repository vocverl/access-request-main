{
  "name": "vitens-access-request-portal",
  "version": "1.4.0",
  "private": true,
  "type": "module",
  "description": "Access Request Portal met GRC-connector voor SAP GRC Access Control 12.0",
  "scripts": {
    "dev": "PORT=8086 node --env-file=.env server/index.js",
    "start": "PORT=8086 node --env-file=.env server/index.js",
    "test": "node --test"
  },
  "dependencies": {
    "express": "^4.21.2",
    "fast-xml-parser": "^4.5.1"
  },
  "engines": {
    "node": ">=22"
  }
}
